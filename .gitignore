# Build de Connect IQ
bin/
*.prg
*.iq
*.iqdbg

# Claves de firma del desarrollador — NUNCA al repo
developer_key*
*.pem
*.der

# Editor / SO
.DS_Store
*.swp

# Workspace de IA (scratch efímero; .ai/tasks/ lo decide el proyecto)
.ai/tmp/

# Secretos del bridge (token ntfy, topics) — fuera del repo
bridge/approver/.env
