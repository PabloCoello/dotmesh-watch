# Build de Connect IQ
bin/
*.prg
*.iq
*.iqdbg

# Claves de firma del desarrollador — NUNCA al repo
developer_key*
*.pem
*.der

# Editor / SO
.DS_Store
*.swp

# Workspace de IA — local, no se versiona (ni tareas ni scratch)
.ai/

# Secretos del bridge (token ntfy, topics) — fuera del repo
bridge/approver/.env
